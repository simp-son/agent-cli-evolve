[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"

[[deploy.volumes]]
mountPath = "/data"

[variables]
PORT = "8080"
HL_TESTNET = "false"
APEX_PRESET = "nano"
RUN_MODE = "apex"
DATA_DIR = "/data"
REFLECT_HOUR = "4"
